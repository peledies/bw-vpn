name: Reusable workflow action example

on:
  workflow_call:
    # inputs:
    #   environment_name:
    #     required: true
    #     type: string
    secrets:
      SUPER_SECRET:
        required: true

jobs:
  example_job:
    name: Run Sub Workflow
    runs-on: ubuntu-latest
    # environment: ${{ inputs.environment_name }}
    # env:
    #   SUPER_SECRET: ${{ secrets.SUPER_SECRET }}
    steps:
      - name: Do Build Things
        run: echo "Doing Build Things Here for environment > ${{ secrets.SUPER_SECRET }}"
